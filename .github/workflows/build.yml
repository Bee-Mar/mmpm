name: MMPM Build Pipeline

on:
  push:
    branches:
      - "*"
    tags:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  mmpm-cli:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11"]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Cache Python Dependencies
        uses: actions/cache@v3
        with:
          python-version: ${{ matrix.python-version }}
          path: |
            ~/.cache/pip
            ./.venv
          key: ${{ runner.os }}-python-${{ hashFiles('**/pdm.lock') }}
          restore-keys: |
            ${{ runner.os }}-python-${{ hashFiles('**/pdm.lock') }}
      - name: Python Dependencies
        run: |
          python -m pip install pdm
          pdm install
      - name: Python Tests
        run: |
          pdm run lint
          pdm run mypy
          pdm run test
          pdm run report
      - name: Python Build
        run: |
          pdm build
      - name: Cache Python Build Artifacts
        uses: actions/cache@v3
        with:
          path: |
            ./dist
            ./pdm.lock
          key: ${{ runner.os }}-build-${{ hashFiles('**/pdm.lock') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ hashFiles('**/pdm.lock') }}
      - name: Install Test
        run: |
          pdm install
          .venv/bin/mmpm version
          .venv/bin/mmpm --help

  mmpm-ui:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["16.x", "18.x", "20.x"]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache Node Modules
        uses: actions/cache@v3
        with:
          path: |
            './ui/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Angular Dependencies
        run: |
          cd ui && npm install --legacy-peer-deps && cd ..

      - name: Angular Tests
        run: |
          cd ui && npm run lint && cd ..

      - name: Angular Build
        run: |
          cd ui && ./node_modules/@angular/cli/bin/ng.js build --configuration production --output-hashing none --base-href / && cd ..

      - name: Cache Angular Build Artifacts
        uses: actions/cache@v3
        with:
          path: |
            ./ui/build
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

  package-and-publish:
    needs: [mmpm-cli, mmpm-ui]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Restore Python Dependencies Cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/pip
            ./.venv
          key: ${{ runner.os }}-python-${{ hashFiles('**/pdm.lock') }}
          restore-keys: |
            ${{ runner.os }}-python-${{ hashFiles('**/pdm.lock') }}

      - name: Restore Python Build Artifacts Cache
        uses: actions/cache@v3
        with:
          path: |
            ./dist
          key: ${{ runner.os }}-build-${{ hashFiles('**/pdm.lock') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ hashFiles('**/pdm.lock') }}

      - name: Restore Angular Build Artifacts Cache
        uses: actions/cache@v3
        with:
          path: |
            ./ui/build
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Package MMPM
        run: |
          python -m pip install pdm
          pdm install
          mkdir -p ./mmpm/ui
          cp -r ./ui/build/* ./mmpm/ui
          pdm build

      - name: Publish to PyPi
        if: github.ref == 'refs/heads/master' && startsWith(github.ref, 'refs/tags/')
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_TOKEN }}
          skip_existing: true
          verbose: true
