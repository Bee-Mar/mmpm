language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

before_install:
    - python --version
    - pip install -U pip
    - pip --version
    - pip install codecov

install:
  - pip install codecov
  - pip install -r requirements.txt
  - pip install .

after_success:
  - codecov
