export const environment = {
  production: false,
  // Port of the MMPM Socket.IO repeater process. Override before building
  // if your deployment exposes the repeater on a different port.
  socketPort: 8907,
};
