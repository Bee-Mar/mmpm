import { MagicMirrorPackage } from './magic-mirror-package';

describe('MagicMirrorPackage', () => {
  it('should create an instance', () => {
    //expect(new MagicMirrorPackage()).toBeTruthy();
  });
});
